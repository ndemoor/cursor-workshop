# 3. Quick Script for DevOps Automation (Dockerized)

**What to Do**

1. Revisit your script from the previous exercises (either the “Hello World” or the code snippet with `.cursorrules`).
2. Create a Dockerfile that packages your script into a container. For instance:
   - Use Python as a base image.
   - Copy your script into the container.
   - Install any dependencies.
   - Set the container’s entrypoint to your script.
3. Use the **Terminal CMD+K** feature in Cursor to run your Docker commands directly, such as:
4. Iterate on your Dockerfile by asking **Cursor Chat** to optimize or refine your container (e.g., smaller image size, multi-stage builds, or environment variables).

**Expected Outcome**

- A functional Docker image that runs your script.
- Familiarise yourself with:
  - Terminal CMD+K integration in Cursor for quick commands writing.
  - Using **Cursor Chat** to optimize Docker images and container workflows.

**Hint**

- If you’re unsure about any Docker commands, open Cursor Chat and ask: “What’s the correct Docker build command?” or “How do I reduce this image’s size?”
- Experiment with different base images (e.g., `alpine`) to see how the size changes.

**Estimated Time**

⏰ ~15–20 minutes
